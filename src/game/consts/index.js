const Instances = {
    game: {
        width: 400,
        height: 460,
        preload: "preload",
        engine: "engine",
    },
};

export const { game } = Instances;
export const { width, height, boot, preload, engine } = game;
